#!/bin/bash

#export NODE_OPTIONS=--openssl-legacy-provider && truffle compile && truffle migrate -f 010 --to 080 --network bsc_main
#export NODE_OPTIONS=--openssl-legacy-provider && truffle compile && truffle migrate -f 090 --to 110 --network bsc_main

#Contractman
#export NODE_OPTIONS=--openssl-legacy-provider && truffle compile && truffle migrate -f 001 --to 001 --network bsc_main

#Util
#export NODE_OPTIONS=--openssl-legacy-provider && truffle compile && truffle migrate -f 010 --to 010 --network bsc_main

#RNG
#export NODE_OPTIONS=--openssl-legacy-provider && truffle compile && truffle migrate -f 030 --to 030 --network bsc_main

#Distrib
#export NODE_OPTIONS=--openssl-legacy-provider && truffle compile && truffle migrate -f 060 --to 060 --network bsc_main

#Stats
#export NODE_OPTIONS=--openssl-legacy-provider && truffle compile && truffle migrate -f 070 --to 070 --network bsc_main

#Minter
#export NODE_OPTIONS=--openssl-legacy-provider && truffle compile && truffle migrate -f 080 --to 080 --network bsc_main

#Viewer
export NODE_OPTIONS=--openssl-legacy-provider && truffle compile && truffle migrate -f 090 --to 090 --network bsc_main

#DragonView
#export NODE_OPTIONS=--openssl-legacy-provider && truffle compile && truffle migrate -f 100 --to 100 --network bsc_main

#Treasury
#export NODE_OPTIONS=--openssl-legacy-provider && truffle compile && truffle migrate -f 110 --to 110 --network bsc_main
